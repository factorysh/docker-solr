---

http:
    http://solr:8983{{.Env.BASE_URL}}select?q=*:*:
        status: 200
        no-follow-redirects: false
        timeout: 1000
        body:
            - /status([^0]+)0/
            - /numFound([^0]+)/
