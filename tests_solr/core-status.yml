---

http:
    http://solr:8983{{.Env.TEST_URL}}:
        status: 200
        no-follow-redirects: false
        timeout: 1000
        body:
            - /.*status.*OK.*/
file:
    /var/lib/solr/data/index:
        exists: true
        filetype: directory
