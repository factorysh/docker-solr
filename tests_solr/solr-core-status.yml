---

http:
    # check core status
    http://solr:8983{{.Vars.base_url}}admin/ping?wt=json:
        status: 200
        no-follow-redirects: false
        timeout: 1000
        body:
            - /"status":"OK"/
